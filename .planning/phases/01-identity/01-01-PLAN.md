---
phase: 01-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "README opens with a plain-language statement that this is a static-first starter kit for developers"
    - "README does not mention SSR, Workers, or edge rendering as current capabilities anywhere"
    - "README does not use emoji in headers or section titles"
    - "The Roadmap/Phase 2 SSR section at the bottom is removed entirely"
    - "A developer reading just the first 10 lines knows what this repo is and who it is for"
  artifacts:
    - path: "README.md"
      provides: "Repo identity and developer-facing documentation"
      contains: "static-first starter kit"
  key_links:
    - from: "README.md title/opening"
      to: "developer understanding"
      via: "first 10 lines"
      pattern: "static-first starter kit"
---

<objective>
Rewrite the README opening, headers, and framing to establish a clear static-first starter kit identity. Remove the SSR roadmap section and emoji headers.

Purpose: A developer landing on this repo immediately understands it is a static-first starter kit — not a platform, not an SSR framework, not an editorial CMS.
Output: A tightened README.md with clear identity, professional headers, and no SSR references as current or planned capabilities.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README identity and remove SSR references</name>
  <files>README.md</files>
  <action>
Edit README.md with these specific changes:

1. **Replace the title and opening paragraph:**
   - Old title: `# 🚀 Drupal + Astro + Cloudflare Starter Kit`
   - New title: `# Drupal + Astro Starter Kit` (no emoji)
   - Old subtitle: `Local Drupal CMS → Astro Static Site → Cloudflare Pages`
   - New subtitle: `A static-first starter kit for developers. Local Drupal CMS, Astro static site generator, Cloudflare Pages hosting.`
   - Rewrite the opening paragraph to lead with identity: "This is a static-first starter kit that turns Drupal 11 into a local-only content source and uses Astro to generate a fully static site. The production site is pre-rendered HTML served from Cloudflare Pages — no server-side rendering, no runtime CMS dependency, no edge workers."
   - Keep the "built for Drupal developers" line but remove the "Twig templating hell" phrasing — replace with something like: "Built for developers who want Drupal's content modeling with a modern static frontend."

2. **Remove all emoji from section headers:**
   - `## 🌟 What You Get` → `## What You Get`
   - `## 📋 Table of Contents` → `## Table of Contents`
   - `## ✨ Features` → `## Features`
   - `## 🧰 Prerequisites` → `## Prerequisites`
   - `## 🚦 Quick Start` → `## Quick Start`
   - `## 🧱 Local Development Flow` → `## Local Development Flow`
   - `## 📦 Build & Deployment` → `## Build & Deployment`
   - `## 🧬 Architecture` → `## Architecture`
   - `## 🧪 Example Drupal → Astro Route` → `## Example Drupal → Astro Route`
   - `## 📁 Project Structure` → `## Project Structure`
   - `## 🩺 Troubleshooting` → `## Troubleshooting`
   - `## 📝 License` → `## License`
   - Also remove the `🟩` markers from the Features subsections — use `###` subheaders instead:
     - `🟩 Static-first (default)` → `### Static-first (default)`
     - `🟩 One-command Bootstrap` → `### One-command bootstrap`
     - `🟩 Modern Frontend for Drupal` → `### Modern frontend for Drupal`
     - `🟩 Clean Separation` → `### Clean separation`

3. **Delete the entire "Roadmap (Phase 2: Workers SSR)" section at the bottom** (lines 373-377). This section references SSR as future work with a local file path. Remove it completely — do NOT replace it with anything. The License section should be the last section.

4. **In the Features section**, the "Static-first (default)" subsection already says the right things. Keep it as-is (minus the emoji marker).

5. **In the Troubleshooting section**, the "Cloudflare build fails" entry correctly says "Cloudflare cannot reach your local Drupal. You must build locally, then deploy static output." — keep this, it reinforces the static-first model.

Do NOT change:
- The Quick Start content (clone URL, setup steps)
- The Architecture section content
- The Build & Deployment section content
- The Local Development Flow content
- The Project Structure content
- The Example route code block
  </action>
  <verify>
1. `head -5 README.md` shows title without emoji, subtitle mentions "static-first starter kit"
2. `grep -c '🚀\|🌟\|📋\|✨\|🧰\|🚦\|🧱\|📦\|🧬\|🧪\|📁\|🩺\|📝\|🔮\|🟩' README.md` returns 0
3. `grep -ci 'workers ssr\|phase 2.*ssr\|roadmap.*ssr\|edge rendering' README.md` returns 0
4. `grep -c 'static-first starter kit' README.md` returns at least 1
5. The last section before EOF is `## License`
  </verify>
  <done>
README opens with "static-first starter kit" identity. No emoji in headers. No SSR/Workers references as current or future capabilities. The Roadmap section is gone. A developer reading the first 10 lines knows exactly what this repo is.
  </done>
</task>

</tasks>

<verification>
1. README.md title is `# Drupal + Astro Starter Kit` (no emoji)
2. First paragraph contains "static-first starter kit"
3. Zero emoji characters in the file
4. Zero references to SSR, Workers, or edge rendering as capabilities
5. File ends with License section (no Roadmap section)
6. All existing content sections (Quick Start, Architecture, Build & Deployment, etc.) are preserved with their functional content intact
</verification>

<success_criteria>
- README presents a clear, professional static-first starter kit identity
- No emoji in any header or section marker
- No SSR/Workers/edge rendering mentioned as current or future capability
- All functional documentation (setup, build, deploy, troubleshoot) preserved
- A developer reading the first 10 lines understands: what this is (starter kit), what it does (static site from Drupal), who it's for (developers)
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity/01-01-SUMMARY.md`
</output>
