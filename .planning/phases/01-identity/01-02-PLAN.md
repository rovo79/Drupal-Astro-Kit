---
phase: 01-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [AGENTS.md]
autonomous: true

must_haves:
  truths:
    - "AGENTS.md explicitly identifies this repo as a static-first starter kit"
    - "AGENTS.md states what this repo is NOT (not a platform, not an SSR framework, not an editorial CMS)"
    - "The outdated ExecPlans section referencing .agent/PLANS.md is removed"
    - "The Pointers section does not reference legacy SSR-era docs without clear 'not V1' labeling"
    - "GitHub repo description says 'static-first starter kit'"
  artifacts:
    - path: "AGENTS.md"
      provides: "Agent instructions with tightened identity"
      contains: "static-first starter kit"
  key_links:
    - from: "AGENTS.md identity section"
      to: "agent behavior"
      via: "What this repo is / is not"
      pattern: "static-first starter kit"
---

<objective>
Update AGENTS.md to reflect the tightened static-first starter kit identity, remove outdated references, and update the GitHub repo description.

Purpose: Agents (Claude) working in this repo immediately understand the identity constraints — what this is, what it is not, and what the default architecture is. The GitHub repo description matches.
Output: A tightened AGENTS.md and updated GitHub repo description.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten AGENTS.md identity and remove outdated sections</name>
  <files>AGENTS.md</files>
  <action>
Edit AGENTS.md with these specific changes:

1. **Expand "What this repo is" section to include identity and anti-identity:**

   After the existing bullet "This is a **generator repo** (tooling + templates), not the final app.", add a new line:
   - `- This is a **static-first starter kit** for developers — not a platform, not an SSR framework, not an editorial CMS.`

   After the existing "Prefer changing source-of-truth files" bullet, add a "What this repo is NOT" sub-section:
   ```
   ### What this repo is NOT

   - Not an SSR framework — there is no server-side rendering, no Workers runtime, no edge rendering
   - Not an editorial platform — Drupal is a local-only content source, not a hosted CMS
   - Not a CI/CD pipeline — the default is local build + manual deploy
   ```

2. **Remove the ExecPlans section entirely:**
   Delete the following block:
   ```
   # ExecPlans

   When writing complex features or significant refactors, use an ExecPlan (as described in .agent/PLANS.md) from design to implementation.
   ```
   The `.agent/PLANS.md` reference is outdated — the project now uses `.planning/` for planning artifacts.

3. **Update the Pointers section** to remove the direct legacy doc references and instead point to the docs/ directory with a clearer note:
   Replace the current Pointers section with:
   ```
   ## Pointers (source of truth)

   - Overview: `README.md`
   - AI-maintained maps/checklists: `docs/ai/**` (start with `docs/ai/CODEBASE_MAP.md` and `docs/ai/COMMANDS.md`)
   - Planning artifacts: `.planning/` (roadmap, phase plans, state)
   - Legacy docs exist in `docs/` — files like `ssr-guide.md`, `cloudflare-setup.md`, `github-actions.md` are **not V1 architecture**; they carry Phase 2 banners and should not inform current work
   ```

4. **In the "Default architecture (static-first)" section**, the content is already accurate. Keep it as-is.

5. **In the "Golden rules" section**, the content is already accurate. Keep it as-is.

Do NOT change:
- The "Default architecture (static-first)" section
- The "Golden rules" section
- The "Common commands" section
- The "Testing & validation" section
  </action>
  <verify>
1. `grep -c 'static-first starter kit' AGENTS.md` returns at least 1
2. `grep -c 'NOT' AGENTS.md` returns at least 1 (the "What this repo is NOT" section)
3. `grep -c 'ExecPlan' AGENTS.md` returns 0
4. `grep -c '.agent/PLANS.md' AGENTS.md` returns 0
5. `grep -c '.planning/' AGENTS.md` returns at least 1
  </verify>
  <done>
AGENTS.md explicitly identifies the repo as a static-first starter kit, states what it is NOT, has no outdated ExecPlans reference, and points to the current planning system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update GitHub repo description</name>
  <files></files>
  <action>
Run the following command to update the GitHub repo description:

```bash
gh repo edit --description "Static-first starter kit: local Drupal CMS + Astro static site generator + Cloudflare Pages hosting"
```

If this fails due to auth or permissions, log the failure and note it as a manual follow-up — do not block the plan on it. The AGENTS.md update is the primary deliverable.
  </action>
  <verify>
1. Run `gh repo view --json description -q .description` and confirm it contains "static-first starter kit"
2. If the `gh` command failed, verify the failure is logged and noted as a manual follow-up
  </verify>
  <done>
GitHub repo description says "Static-first starter kit" (or a manual follow-up is documented if auth prevents it).
  </done>
</task>

</tasks>

<verification>
1. AGENTS.md contains "static-first starter kit" in the identity section
2. AGENTS.md contains a "What this repo is NOT" section listing SSR, editorial platform, and CI/CD
3. AGENTS.md does not reference ExecPlans or .agent/PLANS.md
4. AGENTS.md Pointers section references .planning/ and labels legacy docs as "not V1 architecture"
5. GitHub repo description contains "static-first starter kit" (or manual follow-up documented)
</verification>

<success_criteria>
- AGENTS.md presents clear identity: static-first starter kit
- AGENTS.md states what the repo is NOT (SSR, platform, CI/CD pipeline)
- Outdated ExecPlans section removed
- Pointers section updated with current planning references
- GitHub repo description aligned with identity (best-effort via `gh` CLI)
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity/01-02-SUMMARY.md`
</output>
