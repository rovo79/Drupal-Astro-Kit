---
phase: 04-build-source-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - docs/deployment.md
  - docs/architecture.md
autonomous: true

must_haves:
  truths:
    - "A developer reading .env.example sees local-first vars as primary and Cloudflare/deployment vars as clearly optional"
    - "docs/deployment.md labels CI/hosted paths as optional advanced territory, not mixed with the default flow"
    - "docs/architecture.md frames local-first as the V1 operating model"
    - "A developer setting up for the first time follows a local-first path without encountering CI/hosted instructions as if they were required"
  artifacts:
    - path: ".env.example"
      provides: "Environment config with clear local-first vs optional grouping"
      contains: "Optional"
    - path: "docs/deployment.md"
      provides: "Deployment guide with CI/hosted clearly labeled advanced"
      contains: "Optional Advanced"
    - path: "docs/architecture.md"
      provides: "Architecture doc with local-first operating model statement"
      contains: "local-first"
  key_links:
    - from: ".env.example"
      to: "docs/deployment.md"
      via: "Comment referencing deployment guide for optional vars"
      pattern: "deployment"
---

<objective>
Declare local-first as the V1 default and clearly label CI/hosted paths as optional advanced.

Purpose: A developer setting up this kit should immediately understand that local DDEV + local build + manual deploy is THE supported path. CI, hosted Drupal, and automated pipelines are possible but clearly optional. Currently these paths are mixed together — .env.example lists all vars without distinction, and deployment.md's CI section is labeled "Optional" but sits inline with the primary flow.
Output: Updated .env.example with clear grouping, tightened deployment.md and architecture.md with local-first framing.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.env.example
@docs/deployment.md
@docs/architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure .env.example with local-first grouping</name>
  <files>.env.example</files>
  <action>
    Rewrite `.env.example` to clearly separate local-first required vars from optional deployment/advanced vars. Keep all existing vars but reorganize into clear sections.

    New structure:

    ```
    # Drupal_Astro_Kit Environment Configuration
    # Copy this file to .env and update the values for your environment
    #
    # V1 Operating Model: Local-first
    # Drupal runs locally via DDEV. Astro builds a static site locally.
    # You deploy the static output manually to Cloudflare Pages.

    # ─── Required: Local Development ────────────────────────────────
    # These are the only vars you need to get started.

    PROJECT_NAME=your-project-name

    # Drupal (local DDEV instance)
    DRUPAL_BASE_URL=http://your-project-name.ddev.site
    DRUPAL_JSONAPI_URL=http://your-project-name.ddev.site/jsonapi
    DRUPAL_ADMIN_USER=admin
    DRUPAL_ADMIN_PASS=admin

    # Astro (static site generator)
    API_BASE_URL=http://your-project-name.ddev.site
    HOMEPAGE_ALIAS=/home

    # ─── Optional: Deployment to Cloudflare Pages ───────────────────
    # Only needed when you're ready to deploy. See docs/deployment.md.

    CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
    CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
    CLOUDFLARE_PROJECT_NAME=your-project-name

    # ─── Optional Advanced: Development Tools ───────────────────────
    # Enable these for local development debugging. Not required.

    ENABLE_XDEBUG=false
    ENABLE_MAILHOG=false
    ENABLE_ADMINER=false

    # ─── Optional Advanced: Performance Tuning ──────────────────────
    # Adjust these based on your server capacity. Defaults work fine.

    PHP_MEMORY_LIMIT=256M
    PHP_MAX_EXECUTION_TIME=60

    # ─── Deprecated ─────────────────────────────────────────────────
    # DRUPAL_API_URL — Use DRUPAL_JSONAPI_URL instead
    # NODE_ENV — Not used by V1 static build
    # DRUPAL_ENV — Not used by V1 static build
    ```

    Key changes from current .env.example:
    1. Added V1 operating model header explaining local-first
    2. Grouped "Required: Local Development" vs "Optional: Deployment" vs "Optional Advanced"
    3. Removed NODE_ENV and DRUPAL_ENV from active vars (moved to Deprecated section — they're not used by the V1 static build)
    4. Removed DRUPAL_API_URL from active vars (it was already marked DEPRECATED)
    5. Added reference to docs/deployment.md for Cloudflare setup details
    6. Visual separators between sections

    Do NOT change any actual values — just reorganize and label.
  </action>
  <verify>
    - `grep 'Local Development' .env.example` matches (required section exists)
    - `grep 'Optional.*Deployment' .env.example` matches (optional deployment section)
    - `grep 'Optional Advanced' .env.example` matches (advanced section)
    - `grep 'Deprecated' .env.example` matches (deprecated section)
    - `grep 'deployment.md' .env.example` matches (cross-reference)
    - All original vars still present (either active or in deprecated section)
  </verify>
  <done>.env.example clearly groups vars into local-first required vs optional deployment vs advanced, with deprecated vars separated.</done>
</task>

<task type="auto">
  <name>Task 2: Label CI/hosted paths as optional advanced in docs</name>
  <files>
    docs/deployment.md
    docs/architecture.md
  </files>
  <action>
    **docs/deployment.md:**

    1. At the very top, after the V1 Static-First callout (line 5), add a one-liner:
       > **V1 Default:** Build locally, deploy manually. CI/hosted Drupal paths are optional advanced territory — see the section at the bottom.

    2. The "CI/CD Automation (Optional)" section (line 92) — strengthen the heading and add a callout:
       Change `## CI/CD Automation (Optional)` to `## CI/CD Automation (Optional Advanced)`
       Add a callout after the heading:
       > **This section is optional.** The V1 operating model is local-first: DDEV for Drupal, local `npm run build`, manual deploy via `./scripts/deploy-frontend.sh`. CI/CD is for teams or projects that have outgrown the manual workflow.

    3. The "Option 2: Hosted Drupal for CI" (line 124) — add a note:
       After "For full CI/CD with automatic rebuilds:" add:
       > **This requires hosting Drupal publicly.** The V1 kit is designed for local-only Drupal. Hosting Drupal is a significant step beyond the starter kit scope.

    **docs/architecture.md:**

    1. In the opening callout (line 5), the text already says "This kit defaults to static site generation." Good — leave it.

    2. The "CI/CD (Optional)" section (line 68) — strengthen:
       Change `### 4. CI/CD (Optional)` to `### 4. CI/CD (Optional Advanced)`
       Add a brief callout after the heading:
       > The V1 operating model is local-first. CI/CD is optional for teams that need automated deploys.

    These are small, targeted edits. The goal is that a developer skimming headings and callouts immediately sees that CI/hosted is NOT the default path.
  </action>
  <verify>
    - `grep 'Optional Advanced' docs/deployment.md` returns match
    - `grep 'local-first' docs/deployment.md` returns match
    - `grep 'Optional Advanced' docs/architecture.md` returns match
    - `grep -i 'local-first' docs/architecture.md` returns match
  </verify>
  <done>CI/hosted paths are clearly labeled "Optional Advanced" in both deployment and architecture docs. Local-first is stated as the V1 default.</done>
</task>

</tasks>

<verification>
- `.env.example` has clear required/optional/advanced grouping
- `docs/deployment.md` has "Optional Advanced" label on CI section and local-first framing
- `docs/architecture.md` has "Optional Advanced" label on CI section
- `grep -c 'Optional' .env.example` returns >= 2 (multiple optional sections)
- `grep 'local-first' docs/deployment.md docs/architecture.md` returns matches in both files
- A first-time developer reading .env.example knows which vars to fill in immediately vs later
</verification>

<success_criteria>
1. Documentation declares local-first as the V1 default operating model
2. CI/hosted build paths are labeled "Optional Advanced" wherever they appear
3. .env.example reflects local-first as primary with clear grouping
4. A developer setting up for the first time follows a local-first path without encountering CI/hosted instructions as required
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-source-model/04-01-SUMMARY.md`
</output>
