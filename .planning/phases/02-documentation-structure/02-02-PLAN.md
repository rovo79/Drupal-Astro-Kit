---
phase: 02-documentation-structure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ai/ARCHITECTURE.md
  - docs/ai/CODEBASE_MAP.md
  - docs/ai/SECURITY_AND_RISKS.md
  - docs/ai/HOTSPOTS.md
  - docs/ai/DEPLOYMENT.md
autonomous: true

must_haves:
  truths:
    - "AI docs do not reference legacy SSR-era doc paths as if they are current"
    - "AI docs reflect the docs/future/ reorganization"
    - "AI docs describe the V1 architecture without presenting SSR as a near-term path"
  artifacts:
    - path: "docs/ai/ARCHITECTURE.md"
      provides: "Updated architecture doc reflecting docs/future/ move"
      contains: "docs/future/"
    - path: "docs/ai/CODEBASE_MAP.md"
      provides: "Updated codebase map reflecting docs/ reorganization"
      contains: "docs/future/"
    - path: "docs/ai/SECURITY_AND_RISKS.md"
      provides: "Updated risks doc with correct paths to legacy docs"
      contains: "docs/future/"
  key_links:
    - from: "docs/ai/ARCHITECTURE.md"
      to: "docs/future/"
      via: "Phase 2 reference section"
      pattern: "docs/future/"
    - from: "docs/ai/CODEBASE_MAP.md"
      to: "docs/future/"
      via: "Docs section in layout"
      pattern: "docs/future/"
---

<objective>
Update AI-maintained docs to reflect the V1 architecture and the docs/future/ reorganization.

Purpose: AI agents reading docs/ai/ should get an accurate picture of the V1 static-first architecture and know that legacy SSR docs now live in docs/future/. Stale references to `docs/ssr-guide.md` or `docs/cloudflare-setup.md` as root-level files will cause confusion.
Output: Updated AI docs with correct paths and consistent V1 framing.
</objective>

<execution_context>
@/Users/rob/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/rob/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AI docs with correct paths and V1 framing</name>
  <files>
    docs/ai/ARCHITECTURE.md
    docs/ai/CODEBASE_MAP.md
    docs/ai/SECURITY_AND_RISKS.md
    docs/ai/HOTSPOTS.md
    docs/ai/DEPLOYMENT.md
  </files>
  <action>
    Update references to legacy SSR-era docs across AI-maintained files. The legacy docs have moved from `docs/` root to `docs/future/`. Update all path references accordingly, and tighten any framing that presents SSR as adjacent/imminent rather than clearly walled-off future work.

    Specific changes per file:

    **docs/ai/ARCHITECTURE.md** (lines 39-48):
    - Line 42: Change `docs/ssr-guide.md`, `docs/cloudflare-setup.md`, `docs/github-actions.md` → `docs/future/ssr-guide.md`, `docs/future/cloudflare-setup.md`, `docs/future/github-actions.md`
    - Line 48: Change `docs/phase-2-workers-ssr.md` → `docs/future/phase-2-workers-ssr.md`
    - Keep the "Phase 2 (Future)" section header and content — just fix paths

    **docs/ai/CODEBASE_MAP.md** (lines 24, 59-61):
    - Line 24: Change `docs/`: description from "user-facing docs (architecture/deployment/troubleshooting/etc)." to "user-facing docs. V1 docs at root (`architecture.md`, `deployment.md`, `troubleshooting.md`); legacy SSR-era docs walled off in `docs/future/`."
    - Lines 59-61: Update the Docs section to reflect the split:
      ```
      - **Docs**
        - V1 user docs: `docs/architecture.md`, `docs/deployment.md`, `docs/troubleshooting.md`
        - Legacy/future reference: `docs/future/` (SSR-era docs, walled off)
        - AI-maintained docs: `docs/ai/`
      ```

    **docs/ai/SECURITY_AND_RISKS.md** (line 31):
    - Change `docs/cloudflare-setup.md` → `docs/future/cloudflare-setup.md`

    **docs/ai/HOTSPOTS.md** (lines 11-13):
    - Update file paths in churn list:
      - `docs/architecture.md` stays (still exists at that path)
      - `docs/deployment.md` stays
      - `docs/troubleshooting.md` stays
      - These are historical churn counts, so paths are fine as-is (they reflect git history). No change needed here actually.
    - BUT: If there's any prose referencing legacy doc paths as current, update those. On review, HOTSPOTS.md doesn't have prose references to legacy doc paths, so no changes needed to this file.

    **docs/ai/DEPLOYMENT.md** (no legacy doc references found on review — skip if no changes needed)

    Summary: The main files needing edits are ARCHITECTURE.md, CODEBASE_MAP.md, and SECURITY_AND_RISKS.md. Only edit HOTSPOTS.md and DEPLOYMENT.md if they contain stale legacy doc paths (they likely don't based on review).
  </action>
  <verify>
    - `grep -rn 'docs/ssr-guide\|docs/cloudflare-setup\|docs/github-actions\|docs/phase-2-workers' docs/ai/` should return ZERO matches for bare `docs/` paths (all should be `docs/future/` now)
    - `grep -n 'docs/future/' docs/ai/ARCHITECTURE.md` returns matches showing updated paths
    - `grep -n 'docs/future/' docs/ai/CODEBASE_MAP.md` returns matches showing updated paths
  </verify>
  <done>All AI-maintained docs reference legacy files at their correct docs/future/ paths. No AI doc presents SSR-era content as current V1 architecture.</done>
</task>

</tasks>

<verification>
- `grep -rn 'docs/ssr-guide\.md\|docs/cloudflare-setup\.md\|docs/github-actions\.md\|docs/phase-2-workers-ssr\.md' docs/ai/` returns empty (no stale root-level paths)
- `grep -c 'docs/future/' docs/ai/ARCHITECTURE.md` returns >= 1
- `grep -c 'docs/future/' docs/ai/CODEBASE_MAP.md` returns >= 1
- `grep -c 'docs/future/' docs/ai/SECURITY_AND_RISKS.md` returns >= 1
</verification>

<success_criteria>
1. AI-maintained docs reference legacy files at docs/future/ paths, not docs/ root
2. AI docs describe the V1 architecture without presenting SSR as adjacent or imminent
3. CODEBASE_MAP reflects the docs/ directory reorganization (V1 at root, legacy in future/)
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-structure/02-02-SUMMARY.md`
</output>
