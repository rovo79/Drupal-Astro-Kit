name: 'DAK: Decoupled base'
description: 'Baseline modules for a decoupled Drupal instance serving JSON:API for Astro SSG.'
type: 'Site'

install:
  - jsonapi
  - path
  - pathauto

config:
  import:
    pathauto:
      - pathauto.pattern.page
